import React from 'react'

const Header = () => {
    return (
        <header style={{ backgroundColor: 'lightblue', padding: '1rem' }}>
            <p>Header</p>
        </header>
    )
}

export default Header